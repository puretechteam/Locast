# Shared crypto crate

Ed25519 signature helpers, blake3 and SHA-256 streaming hashers, and the canonical-JSON signer used by the Locast manifest. P0-T01 registers this as a Cargo workspace member; P3+ adds the real implementations per `docs/ARCHITECTURE.md` section 8 and section 26.4.
